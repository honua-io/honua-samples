{
  "id": "odata-query-rest",
  "title": "OData v4 filtered query (plain REST)",
  "description": "Imports a tiny GeoJSON dataset, publishes it, and walks Honua's OData v4 service -- service document, $metadata, and a $filter/$count query -- asserting the filtered result set is exactly the subset it should be.",
  "capabilities": [
    "import.file",
    "serve.odata"
  ],
  "sdks": [
    "rest"
  ],
  "protocols": [
    "odata"
  ],
  "edition": "community",
  "entrypoint": {
    "type": "node",
    "command": "node src/run.mjs"
  },
  "status": "active"
}
